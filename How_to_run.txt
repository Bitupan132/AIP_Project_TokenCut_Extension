1. single image

cd unsupervised_saliency_detection 
python get_saliency.py --sigma-spatial 16 --sigma-luma 16 --sigma-chroma 8 --vit-arch small --patch-size 16 --img-path ../datasets/VOC2007/VOCdevkit/VOC2007/JPEGImages --out-dir ./output

2. Evaulation
python main_tokencut.py --dataset VOC07 --set trainval

updated:
# Baseline — pure cosine similarity
python main_tokencut.py --dataset VOC07 --set trainval --affinity-method baseline

# Method A — spatial continuity constraint
python main_tokencut.py --dataset VOC07 --set trainval --affinity-method A --spatial-weight 5.0 --spatial-sigma 9.0

# Method B — graph diffusion
python main_tokencut.py --dataset VOC07 --set trainval --affinity-method B --diffusion-gamma 0.1

# change 2: recursive ncut

# Standard single-object (unchanged)
python main_tokencut.py --dataset VOC07 --set trainval

# Iterative multi-object
python main_tokencut.py --dataset VOC07 --set trainval --recursive

# Tighter recursion (fewer, more confident boxes)
python main_tokencut.py --dataset VOC07 --set trainval --recursive \
    --ncut-threshold 0.15 --stability-threshold 0.04 --min-segment-size 30

# change 3: multi eigen vector
# Detect 3 objects (default)
python main_tokencut.py --dataset VOC07 --set trainval --multi-eigenvec

# Detect 5 objects, with Method A affinity
python main_tokencut.py --dataset VOC07 --set trainval --multi-eigenvec \
    --n-segments 5 --affinity-method A --spatial-weight 5.0

# Tune k-means reliability
python main_tokencut.py --dataset VOC07 --set trainval --multi-eigenvec \
    --n-segments 4 --kmeans-n-init 20

# k is chosen automatically (up to 6 by default)
python main_tokencut.py --dataset VOC07 --set trainval --auto-k

# Allow up to 4 objects max
python main_tokencut.py --dataset VOC07 --set trainval --auto-k --max-k 4

# Combine with Method B affinity
python main_tokencut.py --dataset VOC07 --set trainval --auto-k \
    --affinity-method B --diffusion-gamma 0.1


python main_tokencut.py \
    --image_path datasets/VOC2007/VOCdevkit/VOC2007/JPEGImages/000012.jpg \
    --recursive \
    --visualize all


python main_tokencut.py --dataset VOC12 --set trainval --output_dir outputs_baseline_avg


# Method A — Spatial Gaussian kernel
python main_tokencut.py \
    --dataset VOC12 --set trainval \
    --affinity-method A \
    --spatial-weight 0.3 \
    --spatial-sigma 5.0 \
    --output_dir outputs_spatial

# Method B — Graph diffusion
python main_tokencut.py \
    --dataset VOC12 --set trainval \
    --affinity-method B \
    --diffusion-gamma 0.1 \
    --output_dir outputs_diffusion


# Recursive with minimum box area argument ->
python main_tokencut.py \
    --dataset VOC12 --set trainval \
    --recursive \
    --ncut-threshold 0.4 \
    --max-segment-ratio 0.8 \
    --min-box-area-ratio 0.02 \
    --output_dir outputs_recursive_minbox


# Diffusion + Recursive with minimum box area argument ->
python main_tokencut.py \
    --dataset VOC12 --set trainval \
    --recursive \
    --ncut-threshold 0.4 \
    --max-segment-ratio 0.8 \
    --min-box-area-ratio 0.02 \
    --affinity-method B \
    --output_dir outputs_recursive_diffusion_minbox


# spatial + recursive + minimum box area -> 
python main_tokencut.py \
    --dataset VOC12 --set trainval \
    --recursive \
    --ncut-threshold 0.4 \
    --max-segment-ratio 0.8 \
    --min-box-area-ratio 0.02 \
    --affinity-method A \
    --spatial-weight 0.3 \
    --spatial-sigma 5.0 \
    --output_dir outputs_recursive_spatial_minbox


python compare_corloc.py \
    --baseline-pkl outputs_baseline_avg/VOC12_trainval/TokenCut-vit_small16_k/preds.pkl \
    --recursive-pkl outputs_recursive_spatial_minbox/VOC12_trainval/TokenCut-vit_small16_k/preds.pkl \
    --out-dir VOC12/comparison_avg_recursive_spatial_minbox

python compare_corloc.py \
    --baseline-pkl outputs_baseline_avg/VOC12_trainval/TokenCut-vit_small16_k/preds.pkl \
    --recursive-pkl outputs_recursive_diffusion_minbox/VOC12_trainval/TokenCut-vit_small16_k/preds.pkl \
    --dataset VOC12 \
    --set trainval \
    --out-dir VOC12/comparison_avg_recursive_diffusion_minbox


python main_tokencut.py \
    --image_path datasets/VOC2007/VOCdevkit/VOC2007/JPEGImages/000112.jpg \
    --affinity-method A \
    --spatial-weight 0.2 \
    --spatial-sigma 5.0 \
    --visualize all

python main_tokencut.py \
    --image_path datasets/VOC2012/VOCdevkit/VOC2012/JPEGImages/2008_000215.jpg \
    --recursive \
    --ncut-threshold 0.4 \
    --max-segment-ratio 0.8 \
    --min-box-area-ratio 0.02 \
    --visualize all




# -------------- Saliency Detection ----------------

cd unsupervised_saliency_detection

# Baseline
python get_saliency.py --out-dir ../outputs_saliency_detection/ECSSD_baseline --dataset ECSSD --vit-arch small --patch-size 16 --nb-vis 200


# Spatial (Method A)
python get_saliency.py \
    --out-dir ../outputs_saliency_detection/ECSSD_spatial \
    --dataset ECSSD \
    --vit-arch small --patch-size 16 \
    --affinity-method A --spatial-weight 0.2 --nb-vis 200

# Graph diffusion (Method B)
python get_saliency.py \
    --out-dir ../outputs_saliency_detection/ECSSD_diffusion \
    --dataset ECSSD \
    --vit-arch small --patch-size 16 \
    --affinity-method B --diffusion-gamma 0.1 --nb-vis 200

# Recursive
python get_saliency.py \
    --out-dir ../outputs_saliency_detection/ECSSD_recursive \
    --dataset ECSSD \
    --vit-arch small --patch-size 16 \
    --recursive --ncut-threshold 0.4 --max-segment-ratio 0.8 --nb-vis 200

# Recursive + diffusion
python get_saliency.py \
    --out-dir ../outputs_saliency_detection/ECSSD_recursive_diffusion \
    --dataset ECSSD \
    --vit-arch small --patch-size 16 \
    --recursive --ncut-threshold 0.4 --max-segment-ratio 0.8 \
    --affinity-method B --diffusion-gamma 0.1 --nb-vis 200

# Recursive + spatial
python get_saliency.py \
    --out-dir ../outputs_saliency_detection/ECSSD_recursive_spatial \
    --dataset ECSSD \
    --vit-arch small --patch-size 16 \
    --recursive --ncut-threshold 0.4 --max-segment-ratio 0.8
    --affinity-method A --spatial-weight 0.3 --nb-vis 200


